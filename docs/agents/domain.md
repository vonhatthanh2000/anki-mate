# Domain Docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` when present and relevant decisions under `docs/adr/`. Missing domain files should not block work.

Use vocabulary defined in `CONTEXT.md` consistently. Surface conflicts with existing ADRs explicitly instead of silently overriding them.

